#include "Quiz.h"

int main()
{
    /*Player* plrs = new Player;
    plrs->setUserName("name");
    plrs->setPassword("pass");
    savePlayerList(FilePath, plrs, 1);*/
    login();
    //createTopic();
    //Question q;
    /*vector<Question> Q;
    loadQuestionList(QUESTION_PATH_DATA, Q);
    cout << "t " << Q.size() << endl;
    for (int i = 0; i < Q.size(); i++)
        Q[i].print();*/
    /*vector<Player> p;
    loadPlayerList(PLAYER_PATH_DATA, p);
    cout << p.size() << endl;
    for (int i = 0; i < p.size(); i++)
        cout << p[i].getUserName() << endl;*/
    return 0;
}